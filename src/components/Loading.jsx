import LoadingBar from '@dimasmds/react-redux-loading-bar'

const Loading = () => {
  return (
        <div className="loading">
            <LoadingBar />
        </div>
  )
}

export default Loading
